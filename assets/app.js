const app=document.getElementById('app');
const hero=document.getElementById('hero');
const backButton=document.getElementById('backButton');
const homeButton=document.getElementById('homeButton');
const modal=document.getElementById('itemModal');
const modalContent=document.getElementById('modalContent');
const closeModal=document.getElementById('closeModal');
let view='home';let selectedArtist='';let selectedEra='';let activeType='All';

function countForArtist(artist){return collection.filter(i=>i.artist===artist).length}
function countForEra(era){return collection.filter(i=>i.artist==='Tate McRae'&&i.era===era).length}
function setView(next){view=next;backButton.classList.toggle('hidden',view==='home');hero.classList.toggle('hidden',view!=='home');window.scrollTo({top:0,behavior:'smooth'});render()}
function renderHome(){app.innerHTML=`<div class="section-head"><div><p class="eyebrow">CHOOSE AN ARCHIVE</p><h2 class="section-title">Explore the collection.</h2></div></div><div class="choice-grid"><button class="choice-card" data-artist="Tate McRae"><div class="choice-visual"><div class="choice-title">Tate McRae</div></div><div class="choice-body"><span class="count">${countForArtist('Tate McRae')} items</span><p>Browse by era, then by Vinyl, CD, Merch and more.</p></div></button><button class="choice-card" data-artist="Other Artists"><div class="choice-visual"><div class="choice-title">Other Artists</div></div><div class="choice-body"><span class="count">${countForArtist('Other Artists')} items</span><p>Everything outside the Tate McRae archive.</p></div></button></div>`;document.querySelectorAll('[data-artist]').forEach(b=>b.addEventListener('click',()=>{selectedArtist=b.dataset.artist;setView(selectedArtist==='Tate McRae'?'eras':'items')}))}
function renderEras(){app.innerHTML=`<div class="section-head"><div><p class="eyebrow">TATE McRAE</p><h2 class="section-title">Choose an era.</h2></div><p class="section-copy">Open an era to browse Vinyl, CD, Merch, Magazines, Passes and other collectibles.</p></div><div class="era-grid">${eras.map(e=>`<button class="era-card" data-era="${e.id}"><img class="era-image" src="${e.image}" alt="${e.title}"><div class="era-body"><span class="count">${countForEra(e.id)} items</span><h3>${e.title}</h3><p>${e.subtitle}</p></div></button>`).join('')}</div>`;document.querySelectorAll('[data-era]').forEach(b=>b.addEventListener('click',()=>{selectedEra=b.dataset.era;activeType='All';setView('items')}))}
function renderItems(){const base=collection.filter(i=>i.artist===selectedArtist&&(selectedArtist!=='Tate McRae'||i.era===selectedEra));const types=['All',...new Set(base.map(i=>i.type))];app.innerHTML=`<div class="section-head"><div><p class="eyebrow">${selectedArtist==='Tate McRae'?(eras.find(e=>e.id===selectedEra)?.title||'TATE McRAE'):'OTHER ARTISTS'}</p><h2 class="section-title">The collection.</h2></div></div><div class="controls"><div class="filters">${types.map(t=>`<button class="filter ${t===activeType?'active':''}" data-type="${t}">${t}</button>`).join('')}</div><input id="search" type="search" placeholder="Search items" aria-label="Search items"></div><div class="item-grid" id="itemGrid"></div>`;const search=document.getElementById('search');document.querySelectorAll('[data-type]').forEach(b=>b.addEventListener('click',()=>{activeType=b.dataset.type;renderItems()}));search.addEventListener('input',()=>renderItemGrid(base,search.value));renderItemGrid(base,'')}
function renderItemGrid(base,term){const grid=document.getElementById('itemGrid');const q=term.toLowerCase().trim();const items=base.filter(i=>(activeType==='All'||i.type===activeType)&&[i.title,i.type,i.edition,i.year].join(' ').toLowerCase().includes(q));grid.innerHTML=items.length?items.map(i=>`<button class="item-card" data-id="${i.id}"><img class="item-image" src="${i.cover}" alt="${i.title}"><div class="item-body"><div class="item-meta"><span>${i.type}</span><span>${i.year}</span></div><h3>${i.title}</h3><p>${i.edition}</p></div></button>`).join(''):'<p class="empty">No items here yet.</p>';document.querySelectorAll('[data-id]').forEach(b=>b.addEventListener('click',()=>showItem(Number(b.dataset.id))))}
function showItem(id){const i=collection.find(x=>x.id===id);if(!i)return;modalContent.innerHTML=`<img class="modal-hero" src="${i.cover}" alt="${i.title}"><div class="modal-body"><p class="eyebrow">${i.type}</p><h2>${i.title}</h2><div class="modal-details"><div class="detail"><span>Edition</span><strong>${i.edition}</strong></div><div class="detail"><span>Year</span><strong>${i.year}</strong></div><div class="detail"><span>Condition</span><strong>${i.condition}</strong></div><div class="detail"><span>Signed</span><strong>${i.signed}</strong></div></div><p class="modal-story">${i.story}</p><div class="gallery">${i.images.map(x=>`<img src="${x}" alt="${i.title}">`).join('')}</div></div>`;modal.showModal()}
function render(){if(view==='home')renderHome();else if(view==='eras')renderEras();else renderItems()}
backButton.addEventListener('click',()=>{if(view==='items'&&selectedArtist==='Tate McRae')setView('eras');else setView('home')});homeButton.addEventListener('click',()=>setView('home'));closeModal.addEventListener('click',()=>modal.close());modal.addEventListener('click',e=>{if(e.target===modal)modal.close()});render();
